{
  "scripts": {
    "build": "esbuild --bundle src/main.js --outdir=dist --minify --loader:.png=file --loader:.svg=file --sourcemap",
    "website": "esbuild --bundle src/main.js --outdir=dist --minify --sourcemap --serve --loader:.png=file --loader:.svg=file --servedir=.",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "esbuild": "0.25.1"
  },
  "dependencies": {
    "chess.js": "^1.1.0",
    "chessground": "^9.1.1"
  }
}
